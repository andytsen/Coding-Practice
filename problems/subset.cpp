#include <iostream>
#include <string>

// get all distinct substrings of a string
//
vector<string> distinctSubstrings(string s) {
    for(int len = 1; len <= s.size(); ++len) {
        for(int i = 0; i <= s.size() - len; ++i) {
            int j = len + i - 1;
        }
    } 
}
int main() {
    string s = "abcd";
}


