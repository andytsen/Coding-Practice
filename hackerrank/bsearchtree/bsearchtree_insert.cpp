#include <iostream>
using namespace std;

/*
 * Node is defined as
 *
 * typedef struct node 
 * {
 *      int data;
 *      node* left;
 *      node* right;
 * } node;
 */

node* insert(node* root, int value)
{
    return root;
}
