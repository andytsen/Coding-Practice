#include <iostream>
#include <vector>

// this is a dp prob

using namespace std;
int main() {
    int T;
    cin >> T;
    vector<int> coins;
    vector<int> dp(0, T-1);
    for(int i = 0; i < T; ++i) {
        int w;
        coins.push_back(w);
    }
    int W;
    cin >> W;

    
}
