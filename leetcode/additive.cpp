#include <iostream>
#include <string>

bool dfs(string num, string a, string b) {
    
}

bool isAdditiveNumber(string num) {
    if(num.size() < 3)
        return false;
    return false;
}

int main() {
    return 0;
}
