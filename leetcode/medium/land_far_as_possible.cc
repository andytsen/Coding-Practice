
#include <vector>

using namespace std;

int farest_land(vector<vector<int>>& grid)
{
    vector<vector<int>> closest_land_dist;
    for(int r = 0; r < grid.size(); ++r) 
    {
        for(int c = 0; c < grid[0].size(); ++c)
        {
            
        }
    }

}


int main() 
{
    return 0;
}
