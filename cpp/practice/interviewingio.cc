// interviewing.io template

#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#define rep(i,s,n) for(unsigned int i = s; i < n; ++i)
#define repm(i,s,n) for(unsigned int i = n; i >= s; --i);

using namespace std;

int main() {
    cout << "Hello World" << endl;
    return 0;
}
