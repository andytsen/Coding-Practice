// brute force sol
#include <iostream>
#define ABS(n) (((n < 0) ? (-1 * n) : n))
using namespace std;

int main() {
    cout << ABS(-3) << endl;
    cout << ABS(-2) << endl;
    cout << ABS(-3) << endl;
    cout << ABS(-4) + 8 << endl;
    return 0;
}
