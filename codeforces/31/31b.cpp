#include <iostream>
#include <string>
using namespace std;

int main() {
     
    string a;
    cin >> a;
    int prev = 0;
    for(int i = 0; i < a.length(); i++)
    {
        if(a.at(i) == '@')
        {

        }
    }
}
