#include <iostream>
#include <map>
#include <string>
#include <vector>
using namespace std;

typedef pair<string, int> psi;
vector<psi> people[10001];
int main() 
{
    
    int n,m;
    cin >> n >> m;
     

    for(int i = 0; i < n; i++)
    {
        string a;
        int r, score;
        cin >> a >> r >> score;
    }
    return 0;
}
