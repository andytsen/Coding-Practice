#include <iostream>
#include <vector>

#define rep(i,n) for(int i = 0; i < n; ++i)

using namespace std;

int main() {
    int n,d;
    cin >> n >> d;
    vector<int> cities = vector<int>(n,0);

    rep(i,n) {
        cin >> cities[i];
    }
    rep(i,n) {

    }
}
