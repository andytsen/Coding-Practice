#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, q;
    cin >> n >> q;
    int apps[n];
    set<pair<int,int>> unread; 

    for(int i = 0; i < q; i++)
    {
        int x, t;
        cin >> x >> t;
        if(x == 1)
        {
        }
        else if(x == 2)
        {
            
        }
        else 
        {

        }
    }    
}
